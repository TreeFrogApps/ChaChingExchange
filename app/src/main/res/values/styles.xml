<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:actionBarSize">56dp</item>
        <item name="colorPrimary">@color/actionBarPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:spinnerItemStyle">@style/mySpinnerItemStyle</item>
        <item name="android:windowContentOverlay">@color/black</item>
        <!-- android:textColorPrimaryInverse is the  color of the title
       text in the Toolbar, in the Theme.AppCompat.Light theme:  -->
        <item name="android:textColorPrimary">@color/abc_primary_text_material_dark</item>
        <!-- android:textColorSecondary is the color of the menu
             overflow icon (three vertical dots) -->
        <item name="android:textColorSecondary">@color/abc_secondary_text_material_dark</item>
        <!-- attributes for editText colors - overriding theme base colors  -->
        <item name="colorControlNormal">@color/editTextGrey</item>
    </style>

    <style name="AppTheme.Base.PopUpMenu.Overlay" parent="ThemeOverlay.AppCompat">
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="PopUpMenu" parent="AppTheme.Base">
        <item name="android:popupBackground">@color/actionBarTextWhite</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/abc_text_size_menu_material</item>
    </style>

    <style name="mySpinnerItemStyle" parent="">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">@dimen/text_bigger</item>
    </style>


</resources>
